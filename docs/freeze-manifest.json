{
  "_doc": "Freeze enforcement manifest (PE-FREEZE-001). The semantic foundation is frozen at `pin` (v1.0.0 single-commit history; prior line squashed \u2014 old HEAD 30b6019, old pin 51ce178/freeze-candidate-1, old tree 8a510f43; frozen paths byte-identical across the squash, verified by green freeze guard pre-squash; holders of pre-squash clones can confirm by tree comparison). Any diff since the pin inside `frozen_paths` must be covered by a `change_records` entry below; anything else fails tools/check_freeze.py / make freeze-guard. CORE-class changes additionally require a docs/ARCHITECTURE-CHANGE-PROPOSAL-*.md per ARCHITECTURE-FREEZE.md \u00a75. Prior per-commit records (13, referencing erased history) are subsumed by the re-baseline record below.",
  "change_records": [
    {
      "class": "DOCUMENTATION",
      "commit": "791502ffaf0c42aa5c33e5ef19b8f9dccb9e90b4",
      "files": [],
      "layer": "v1.0.0 history squash re-baseline: full pre-release tree re-committed as one commit; frozen paths carried over byte-identical (remediation touched CLI/docs/harness only; 52 approved frozen-path diffs since old pin 51ce178 collapse into this baseline; old HEAD 30b6019 / old tree 8a510f43 recorded for audit trail)"
    }
  ],
  "frozen_paths": [
    "crates/proof-core/src/",
    "crates/proof-format/src/",
    "crates/proof-crypto/src/",
    "crates/proof-graph/src/",
    "crates/proof-verify/src/",
    "crates/proof-policy/src/",
    "docs/format.cddl",
    "docs/policy-grammar.ebnf",
    "fixtures/"
  ],
  "pin": "791502ffaf0c42aa5c33e5ef19b8f9dccb9e90b4"
}
